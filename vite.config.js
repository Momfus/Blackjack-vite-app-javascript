
// Configuración usada fuerta de despliegue de vite (como en github pages) y se configura aca las rutas para los recursos
// Al constuir el proyecton con npm run build, se usa estas configuraciones (como agregar plugins, varibles de entorno, etc). Más en la documentación de vite.
import { defineConfig } from 'vite';

export default defineConfig({
   base: '/Blackjack-vite-app-javascript/'
});